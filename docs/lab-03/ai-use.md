# Lab 3 AI use record

## Recorded drafting activity

2026-09-17: ใช้ Codex ช่วยอ่าน ZIP main และตัวอย่าง pipeline, ตรวจ schema/API/theme/test setup,
อ่านใบงาน Lab 3 แล้วร่าง pipeline 5 เฟสและ contracts ที่ตรงกับ source หลัก
ยังไม่ได้ใช้รอบนี้ implement หรือทดสอบระบบ Lab 3 และยังไม่มี student reflection
ชื่อ model แบบละเอียดสำหรับบันทึกส่งงาน: ให้ตรวจค่าจริงจาก session UI เมื่อเก็บหลักฐาน

Prompt หลักในรอบนี้ (สรุป): “ร่าง pipeline แบบตัวอย่างด้านขวา โดยใช้โปรเจกต์ main ด้านซ้าย
เป็นหลัก ขอ ZIP ที่มีโฟลเดอร์ lab-03 พร้อมใช้”

## บันทึกระหว่างพัฒนา

เลือก 6–10 key prompts ที่ใช้จริง พร้อมวันที่/model, purpose, relevant output,
การตรวจและแก้ของนักศึกษา และผลที่เชื่อมโยงกับ commit/tests
ข้อความตัวอย่างใน PROMPTS.md เป็น prompt templates ไม่ใช่หลักฐานว่าเคยส่งแล้ว

## My Reflection

ยังไม่ได้เขียน: นักศึกษาเขียนเองจากประสบการณ์จริง โดยเปรียบเทียบ specification agent/coding agent,
ข้อผิดพลาดที่พบ วิธีตรวจสอบ การตัดสินใจที่ตนรับผิดชอบ และสิ่งที่จะปรับปรุง
ห้ามใช้ข้อความร่างนี้เป็น reflection สำเร็จรูปหรืออ้างว่า implement ผ่านแล้ว
